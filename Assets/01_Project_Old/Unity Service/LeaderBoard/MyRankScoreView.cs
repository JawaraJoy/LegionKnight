using UnityEngine;
using LegionKnight;

namespace Rush
{
    public class MyRankScoreView : RankScoreView
    {
        private void Start()
        {
            //Player.Instance.CurrencyControl.OnCurrencyChanged.AddListener(OnCurrencyChanged);
        }
    }
}
