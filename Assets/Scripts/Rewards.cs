using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rewards : MonoBehaviour
{
    //Random Character Reward Gajadi
    //Kumpulin button Claim Character
    /*public GameObject[] CharacterRewardsButton;

    // Start is called before the first frame update
    public void RewardCharacters()
    {
        int i = Random.Range(0, CharacterRewardsButton.Length);
        CharacterRewardsButton[i].SetActive(true);
    }


    void Start()
    {
        int i = Random.Range(0, CharacterRewardsButton.Length);
        CharacterRewardsButton[i].SetActive(true);
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/

    //Jadinya Random coin reward
    
}
